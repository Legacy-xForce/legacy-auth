{
  "name": "legacy-auth",
  "private": true,
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "dev:backend": "bun run --cwd backend dev",
    "dev:frontend": "bun run --cwd frontend dev",
    "build:frontend": "bun run --cwd frontend build",
    "start": "bun run --cwd backend start",
    "user:add": "bun run --cwd backend user:add",
    "user:set-active": "bun run --cwd backend user:set-active"
  }
}
